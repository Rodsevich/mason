# {{projectName.titleCase()}}

`>` prefix: this README is **always overwritten** when the brick runs,
regardless of conflict resolution settings. Use it for files you treat
as fully owned by the brick.

Generated plugins: {{#plugins}}`{{.}}` {{/plugins}}.
