// `%plugin_register%` — the filename ties this snippet to every site
// annotated with `@GenerateAfter('plugin_register')` in the consumer
// project. masonex will splice the rendered content right after the
// annotated line.
'{{new_plugin_id}}',
