name: loop
description: A new brick created with the Masonexex CLI.

version: 0.1.0+1

environment:
  masonexex: ^0.1.0

vars:
  values:
    type: array
    values:
      - development
      - staging
      - production
